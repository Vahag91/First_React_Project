import Button from "../Button/Button"
import "./add-item.css"

const AddItem = ()=>{
    return (
        <div>
    <input type="text" placeholder="Add new section"/>
    <Button text="Add"/>
        </div>
    )
}

export default AddItem